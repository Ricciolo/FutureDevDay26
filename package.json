{
  "name": "ticket2fix",
  "version": "1.0.0",
  "description": "Custom Developer Agent for support ticket triage and developer handoff.",
  "type": "module",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "dev": "tsc --watch",
    "clean": "node --input-type=module --eval \"import { rmSync } from 'fs'; rmSync('dist', { recursive: true, force: true });\""
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "@types/node": "^20.12.0"
  }
}
